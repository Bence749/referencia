#include <stdio.h>
#include "fib.h"

int main(int argc, char **argv)
{
	/*int number;
	int elso, masodik;
	scanf("%d", &number);
	scanf("%d %d", &elso, &masodik);
	printf("%d, fibonacci number is: %d\n", number, fibonacci(number));
	printf("A nagyobb szám: %d\n", max(elso, masodik));
	int input;
	scanf("%d", &input);
	(isPalidron(input) == 1) ? printf("A szám palidron.\n") : printf("A szám nem palidron\n");*/
	int input2;
	scanf("%d", &input2);
	tillNotOne(input2);-
	return 0;
}

